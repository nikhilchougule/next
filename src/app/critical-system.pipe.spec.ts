import { CriticalSystemPipe } from './critical-system.pipe';

describe('CriticalSystemPipe', () => {
  it('create an instance', () => {
    const pipe = new CriticalSystemPipe();
    expect(pipe).toBeTruthy();
  });
});
